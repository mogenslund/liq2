(ns liq2.experiments.drawing
  (:require [liq2.datastructures.sub-editor :as se]
            [clojure.string :as str]))


; cd src
; lumo -m liq2.experiments.drawing
(defn -main
  []
  (println "Starting drawing test..."))
